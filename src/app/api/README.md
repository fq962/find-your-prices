# api/

Route handlers (`route.ts`) para endpoints propios de esta app (webhooks,
proxies a proveedores externos, etc.). Un `route.ts` aquí solo se vuelve un
endpoint real al crearlo — esta carpeta en sí no expone nada.
