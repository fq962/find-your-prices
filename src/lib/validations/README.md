# validations/

Esquemas de validación (p. ej. con Zod cuando se instale) para formularios,
inputs de server actions y respuestas de APIs externas. Un archivo por
dominio: `product.ts`, `alert.ts`, etc.
