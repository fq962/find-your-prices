# db/

Cliente de base de datos y esquema (Prisma, Drizzle, etc., a definir).
Solo se importa desde código de servidor (`server/`, route handlers, server
actions) — nunca desde `components/` ni `features/*/components`.
