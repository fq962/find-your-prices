# shared/

Componentes compuestos (a partir de `ui/`) que se reutilizan en más de una
feature: por ejemplo `PriceTag`, `EmptyState`, `SearchBar`. Si un componente
solo lo usa una feature, va dentro de `features/<feature>/components`, no aquí.
