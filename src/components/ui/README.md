# ui/

Componentes de UI puros y reutilizables (design system): `Button`, `Input`,
`Card`, `Badge`, etc. No conocen reglas de negocio ni hacen fetch de datos;
solo reciben props y emiten eventos.

Convención de nombre de archivo: `PascalCase.tsx` (uno por componente).
